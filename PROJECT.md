# PROJECT.md

> 이 파일은 **사람이 채웁니다**. 프로젝트 초기에 한 번 작성, 이후 거의 안 바뀜.
> 모든 에이전트가 작업 시작 전 이 파일을 먼저 읽습니다.
> `{...}` 표시된 곳을 본인 프로젝트 정보로 교체하세요.

---

## 기본 정보

- **이름**: `{Project Name}`
- **한 줄 설명**: `{이 프로젝트가 무엇을 하는지 한 문장}`
- **주 사용자**: `{B2C | B2B | 내부 도구}`
- **플랫폼**: `{Web | Mobile | Both}`
- **기본 언어**: `{ko | en | multi}`

## 디자인 기반

- **브랜드 컬러 (hex 1개)**: `{#XXXXXX}`
  > 이 컬러 하나만 입력하면 `/init` 시 `color-scale-builder` 스킬이 50~900 스케일 자동 생성.
- **폰트 패밀리**: `{Pretendard | Inter | SUIT | Geist | ...}`
- **다크모드 지원**: `{yes | no}`

## Figma 연동 (선택)

- **Figma 파일 키**: `{B138LEBl3Li7yNGdSpIuq6 형식, 없으면 비워둠}`
- **페이지 ID — Foundations**: `{0:1}`
- **페이지 ID — Components**: `{87:122}`
- **페이지 ID — Documentation**: `{87:123}`
- **페이지 ID — Wireframes**: `{224:2}`

## 화면설계서 (선택)

> `/screen-spec`로 인터랙션 명세를 만들 때 쓰는 설정. 비워두면 스킬 기본값 사용.

- **Screen ID 포맷**: `{PRJ}-{PLAT}-{CH}-{TYPE}-{SEQ}` (예: `PRD-MO-1-PG-001`)
- **PRJ (프로젝트 코드)**: `{PRD}`
- **PLAT (플랫폼 코드)**: `{MO=Mobile Web, AP=App}`
- **TYPE (화면 유형)**: `{PG=Page, PO=Popup/Bottomsheet, MD=Modal}`
- **Figma Spec 페이지 ID**: `{비우면 Wireframes 페이지 사용}`

## 톤 (자유 입력, 3-5개)

> 단순 형용사보다 **구체적 명사구**가 좋음.
> 좋은 예: "은행 앱처럼 신뢰감", "노션처럼 미니멀", "토스처럼 친근하지만 단정함"
> 나쁜 예: "예쁨", "깔끔함", "현대적"

- `{...}`
- `{...}`
- `{...}`

## 금지 규칙 (선택)

> "절대 안 함" 목록. 이 시스템의 캐릭터를 만드는 데 가장 중요.
> 예: "그림자 사용 금지", "라디우스 8px 초과 금지", "장식용 일러스트 금지", "강조는 색상이 아닌 typography만"

- `{...}`
- `{...}`

## 레퍼런스 (선택)

> 좋아하는 앱/사이트 3-5개. URL이나 이름.

- `{...}`
- `{...}`
- `{...}`

---

## 변경 이력

- `{YYYY-MM-DD}` — 초기 작성
